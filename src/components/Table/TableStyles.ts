import { StyleSheet } from "react-native";

const TableStyles = StyleSheet.create({
  Container: {
    borderWidth: 1,
    borderColor: "gray",
    width: "90%",
    display: "flex",
  },
});

export default TableStyles;
